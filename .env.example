# 이 파일을 .env 로 복사해서 채운다. .env 는 커밋되지 않는다.

# 앱 레포 경로. places.py·verify.py 가 Tour Planner.dc.html 을 읽는다.
APP_REPO=../Tour-Navigator-App

# 공공데이터포털 서비스키 (TourAPI). 디코딩된 원본 키를 넣는다.
# 없으면 재분류 단계만 건너뛴다.
TOURAPI_KEY=
