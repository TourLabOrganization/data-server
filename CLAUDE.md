# data-server

Tour Navigator의 데이터 파이프라인. Python 3.9+, 외부 의존성 없음.

## 작업 전에 읽을 문서

| 지금 하려는 일 | 먼저 읽을 문서 |
|---|---|
| 파이프라인 스크립트 추가·수정 | `docs/pipeline.md` |
| 산출 JSON 스키마 변경 | `docs/contract.md` |
| 데이터랩 CSV 새 탭 쓰기 | `docs/datalab.md` |

문서 간에 같은 규칙이 다르게 적혀 있으면 이 파일을 기준으로 한다.

## 항상 적용

### 1. 원본 데이터를 커밋하지 않는다

- 데이터랩 CSV는 제공처 이용조건상 재배포할 수 없고, **이 레포는 공개 저장소다**
- `data/datalab_raw/` 는 `.gitignore` 가 전부 제외한다. 이 설정을 완화하지 않는다
- TourAPI 응답 캐시(`data/cache/`)도 커밋하지 않는다. 재현 가능하다
- 커밋되는 것은 `data/derived/` 의 계산 결과와 `data/mapping/` 의 변환표뿐이다

### 2. API 키를 코드에 쓰지 않는다

- 공공데이터포털 서비스키는 `.env` 에서만 읽는다. `.env` 는 커밋하지 않는다
- 키가 없으면 그 단계만 건너뛰고, 나머지는 정상 동작하게 만든다
- **한 번 커밋된 키는 지워도 이력에 남는다.** 실수했으면 파일을 고치는 것으로
  끝내지 말고 키를 재발급한다

### 3. 근거 없는 숫자를 만들지 않는다

- 표본이 모자라면 값을 내지 말고 `None` 으로 두고 "측정 불가"라고 적는다.
  빈칸보다 틀린 숫자가 훨씬 위험하다
- 비교 기준(전국 평균 등)이 없으면 정규화하지 않는다
- 단위가 다른 값을 같은 축에 올리지 않는다. 데이터랩의 '체류시간'은 지역 단위
  1회 방문 총량(분)이고 앱의 `min` 은 장소당 권장 시간이라, 직접 비교할 수 없다

### 4. 한글 정규화를 건너뛰지 않는다

- macOS는 한글 파일명을 자모 분리(NFD)로 저장한다. 파일명·장소명을 비교하기
  전에 `common.NFC()` 를 통과시킨다. 빼먹으면 조용히 매칭률만 떨어진다

### 5. 원격에 올리기 전에 사람에게 확인받는다

- 로컬 커밋까지는 확인 없이 진행한다. `git push` 는 실행 전에 확인받는다
- 권한 확인을 건너뛰는 모드에서도 예외를 두지 않는다
- 주 브랜치(`develop`·`main`)에 직접 푸시하거나 force-push하지 않는다

## 브랜치와 커밋

`backend` 레포의 `docs/git.md` 와 같은 규칙을 쓴다.

- 주 브랜치는 `develop`(개발 기준)과 `main`(릴리스 기준) 2개
- 작업은 `<타입>/<기능요약>` 브랜치에서 하고 PR로 머지한다.
  베이스는 `develop`. 예: `feat/tourapi-reclassify`
- 커밋 메시지는 `<타입>: <한국어 요약>` 한 줄. 타입은
  `feat` `fix` `refactor` `docs` `test` `chore` `ci`
- 공동작성(`Co-Authored-By`) 표기는 붙이지 않는다

## 커밋 전

파이프라인을 고쳤으면 `make all` 이 끝까지 도는지 확인한다.
산출물(`data/derived/`)이 바뀌었으면 함께 커밋한다 — 백엔드가 읽는 계약이라
코드와 결과가 어긋나면 안 된다.
