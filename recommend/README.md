# Theme Recommender — Tour Navigator 테마 추천 고도화

메인 화면의 **[테마 추천]** → 선호 문항 14개 → 응답별 점수표로 **여행자 군집(10개)** 판정 → 군집에 맞는 **추천 테마**와 테마별 **코스**를 보여주는 기능의 설계 문서, 근거 데이터, 계산 코드입니다.

## 폴더 구성

```
data-server/recommend/
├── README.md
├── CLAUDE.md                  # Claude Code 작업·push 안내
├── run_all.py                 # 전체 계산 파이프라인
├── requirements.txt
├── docs/
│   ├── 01_고도화_전략.md                 # 전체 전략 (Claude 문서에서 내보냄)
│   ├── 02_테마추천_선호문항_구성안.docx   # 문항 14개, 점수표, 테마 적용, 새 테마 적용법
│   └── 03_군집_산출_시나리오.docx          # 군집 만들기·점수표·가중치 산출 방법과 실제 결과
├── data/
│   ├── 장소_재분류_초안.xlsx      # 앱 장소 1,269곳 주·보조 카테고리 + 성격 태그 (수식 연동)
│   ├── 관광조사_2025_근거표.xlsx  # 국민여행조사·외래관광객조사 수치와 lift·점수 수식
│   ├── derived/                   # 계산 결과 JSON (run_all.py 출력)
│   └── raw/                       # 원자료 넣는 곳 (Git 제외)
└── src/
    ├── survey.py            # 2025 국민여행조사 공표표 → 연령 lift, Q1 점수, 지역 보정
    ├── load_raw.py          # 외래관광객조사 원자료(SAV) 읽기
    ├── foreign_cluster.py   # 외래객 가중 k-평균 (k=6)
    ├── foreign_lift.py      # C7~C10 보기별 lift·점수, 지역·국적 비중
    ├── classify.py          # 장소 주·보조 카테고리·태그 재분류, 테마 구성비
    └── calc2.py             # 군집×테마 적합 점수, 대표 사용자 최종 점수
```

## 실행

```bash
pip install -r requirements.txt
python run_all.py
```

- `classify.py`가 앱의 `assets/tour-places.csv`와 `*.dc.html`을 읽습니다. **`data-server`와 `Tour-Navigator-App`이 같은 폴더에 나란히 있으면** 자동으로 찾고, 아니면 `APP_REPO=/경로/Tour-Navigator-App python run_all.py`.
- 외래관광객조사 원자료가 `data/raw/`에 있으면 외래객 단계까지 다시 계산하고, 없으면 기존 결과를 씁니다.
- 현재 `data/derived/`의 결과는 위 명령으로 똑같이 재현됩니다(검증 완료).

## 핵심 식

| 항목 | 식 |
| --- | --- |
| lift | 군집(또는 연령) 안의 비율 ÷ 전체 비율 |
| 문항 점수 | round(2 × log₂ lift), 0~3 |
| 군집 확률 | 2^(점수 합 ÷ 2)의 비율 |
| 테마 구성비 | 장소별 주 카테고리 0.6 + 보조 0.4 (보조 없으면 1.0) |
| 테마 적합 점수 | Σ(테마 구성비 × 군집 카테고리 가중치) |
| 최종 점수 | 적합 + 0.5 × (관심사 구성비 합 + 야경 태그 비중) + 지역 보정 |
| 지역 보정 | 0.1 × (방문 lift − 1), ±0.05 한도 — 내국인은 연령별, 외래객은 군집별 |

## 군집 (10개)

| 코드 | 군집 | 근거 |
| --- | --- | --- |
| C1~C6 | MZ 트렌드 · 커플 감성 · 키즈 가족 · 액티브 시니어 · 나홀로 힐링 · 미식 로컬 | 2025 국민여행조사 공표표 (연령·동반자) + 가설 |
| C7 | 외래객 K-컬처 (17.3%) | 2025 외래관광객조사 원자료 |
| C8 | 외래객 웰니스 (19.9%) | 〃 |
| C9 | 외래객 자연·지방 (22.8%) | 〃 (k-평균 군집) |
| C10 | 외래객 역사·전통문화 (40.8%) | 〃 (k-평균 3개 군집 합) |

## 데이터 출처

- 2025 국민여행조사 보고서 통계편·분석편 (문화체육관광부·한국관광공사)
- 2025년 외래관광객조사 원자료 — [관광지식정보시스템](https://know.tour.go.kr/stat/fReportsOfForeignerDis19Re.do)
- 한국관광 데이터랩 — https://datalab.visitkorea.or.kr
- 앱 장소 데이터 — 이 레포의 `assets/tour-places.csv`, 테마 화면 `*.dc.html`

## 남은 일

- 점수표의 "가설" 칸(Q3·Q5·Q8 등): 국민여행조사 원자료(MDIS) 확보 후 교체
- 군집 정의 통일: C7·C8(활동 규칙)과 C9·C10(k-평균)을 LCA 하나로
- C10(40.8%) 세분화 여부
- `장소_재분류_초안.xlsx`의 변경 40곳·검토 필요 88곳 확인
