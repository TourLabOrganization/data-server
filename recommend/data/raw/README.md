# 원자료 넣는 곳 (Git에는 올리지 않음)

- `2025년 외래관광객조사_DATA.SAV` — 관광지식정보시스템(know.tour.go.kr) 외래관광객조사 원자료
- 이 폴더의 파일은 `.gitignore`로 제외됩니다. 원자료는 제공처 이용 조건상 재배포가 제한될 수 있습니다.
- 파일이 없으면 `run_all.py`는 외래객 단계를 건너뛰고 `data/derived/fa.json`, `fb.json`(이미 계산된 값)을 그대로 씁니다.
