# API 서버 전용 의존성. 파이프라인(pipeline/)은 여전히 표준 라이브러리만 쓴다.
# 이 둘을 분리해 두면 ETL 을 돌릴 때는 아무것도 설치하지 않아도 된다.
#
# 상한을 두지 않은 이유: Docker 는 python:3.12-slim 으로 고정돼 재현이 보장되고,
# 로컬 파이썬 버전은 사람마다 달라 정확한 핀이 오히려 설치를 막는다.
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.10
